# Table III Final Native Revalidation Evidence

The fixed denominator is 30: six workloads times five fresh-process measured runs. All 15 VWB outcomes map to RELEASE and all 15 VIO outcomes map to BLOCK; there are no UNK, timeout, runner-error, or replacement slots.

RSS values are five-run medians. Runtime and RSS are descriptive measurements from the recorded Ubuntu machine; this is offline software evidence, not a robot trial.

The released validator checks every actual deterministic structural counter against its frozen expectation and checks constancy across each workload's five repetitions.

Each compact slot records the accepted native server-binary identity. The included anonymous server source rebuilt to that same binary on the recorded Ubuntu toolchain. Private raw outputs, stdout/stderr payloads, and their digests are not published.

The public native recount joins every accepted slot to the frozen schedule, public case-input map, exact source/module bytes, actual structural counters, verdict, decision, timing, and RSS. It recounts the accepted records; it does not execute the 30-slot campaign.
