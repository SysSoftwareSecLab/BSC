# Public Correction Ledger

This ledger records corrections that affect how the released evidence must be
interpreted. It intentionally omits private correspondence, author identities,
personal paths, and unrelated engineering history.

| Item | What was found | What was corrected or recomputed | Released conclusion and retained boundary |
|---|---|---|---|
| External-40 live-pose binding | Original outputs exposed two false `VIO` calls caused by a live-pose binding defect. | The binding was corrected and the same admitted set was recomputed. | The reported result is 40/40 oracle agreement: 32 VWB and 8 VIO. External-40 is a development/regression set, not an untouched holdout. |
| External-40 admission-funnel projection | An initial artifact draft incorrectly grouped one mechanically admitted exact-prior canonical-AST duplicate with two parser rejections. | The public funnel was corrected from the retained authoring manifest without changing the scored cohort. | The accurate flow is 43 attempted, 41 mechanically admitted, one admitted prior duplicate excluded, and 40 correctness-eligible/scored. The two true parser rejections remain visible. |
| External-25 repair-era evaluation | The cohort was assembled and recomputed during the repair period rather than frozen as a post-repair untouched sample. | The 25 oracle-valid programs were replayed with the content-addressed final build. | Final verdicts are 7 VWB, 16 VIO, and 2 UNK; 25/25 match the frozen final-revalidation verdicts and 23/25 match the binary oracle. Both unsafe-to-UNK cases remain visible and all 18 unsafe cases are blocked. |
| Controlled extension oracle | The initial independent oracle omitted required static intervals and needed an event-priority correction. | A separate AST/analytic oracle reanalysed the same 24 programs once; the original method runs and prior records were retained. | This is correction-aware reanalysis, not a fresh untouched cohort. The oracle does not import the tested parser/checker or consume its verdict, state, or witness. |
| Controlled extension generation target | The exact executed prompts inherited a 1--16 API-call manual line while Q4 task cards requested 12--24 source actions. The Q4 admission worker separately allowed 1--24. | No prompt, program, slot, or method result was replaced. The public record preserves the contradictory prompt bytes, the actual 1--24 admission rule, and per-slot action counts. | Three 18/19-call programs remain admitted. Q4-SLOT-10 has 10 calls, so 23/24 meet the 12--24 target; all 24 meet the structural target. The count target was not a correctness exclusion rule. |
| IR-sampler matched replay | Two ordinary cases had stale historical release and rollout classifications under the earlier adapter. | The two affected sources were replayed under the corrected matched adapter; 36 affected sources/36,000 rollout rows were checked and 29 unaffected records were reused. | At each published ordinary-cohort budget, the corrected counts are 24 violation observed, 39 no violation observed (not a certificate), and two unresolved/blocked. Rare-40 was unaffected and was not rerun; its public slice is aggregate-only. |
| Native distance-error qualification | A historical native entry point did not include the later entity-indexed candidate/reference distance-error comparator. That omission could admit an erroneous VWB. | The comparator was connected to the final native entry point and qualified at the recorded `1e-12 m` threshold. The final build was then used for the named Table III and C115 offline reruns. | The final named reruns use the corrected build. The public package preserves the accepted qualification records and checks the distance-comparator binding; it does not re-execute the complete qualification. Jointly wrong candidate and reference contracts remain outside this check, so reference correctness is trusted. |
| Table III final-build rerun | Historical measurements did not by themselves establish identity with the corrected final build. | Six frozen workloads were run in five fresh measured processes each on the final Ubuntu build, with no replacement. | 30/30 formal slots preserved their expected verdict and decision, with no UNK, timeout, or runner error. New runtime and memory values belong to the recorded Ubuntu machine and replace, rather than validate, older timing values. |
| Table III RSS summary | An initial release-table transcription used the per-workload maximum although the table column required the median of five measured runs. | RSS was recomputed from all 30 formal slot records and the six medians were restored. | The correction affects the descriptive memory numbers, not any program verdict or release decision. The final public summary must be generated from per-slot records. |
| Table III structural-counter projection | The first compact-export validator compared the repeated frozen expected-counter object rather than the actual counters emitted by each of the five slots. That check could have reported constancy without testing the measured values. | The integration and public recount now check every actual deterministic counter against its frozen expectation and compare the actual structural-counter vector across all five repetitions. | All six workloads pass the corrected check. Verdicts, decisions, runtime, RSS, and counters did not change; only the public validation logic and summary binding were corrected. |
| C115 final offline revalidation | The corrected final implementation had not previously been joined to the complete frozen 15-slot C/H/R schedule in one final-build record. | All 15 scheduled identities were recomputed offline with the final build. Two controlled-program negatives and 15 family-matched native negative repetitions were retained, and the zero-send boundary was checked. | 15/15 scheduled identities passed; every retained unsafe control produced VIO/BLOCK. This validates the current frozen software evidence and does not alter the historical hardware denominator or complete the historical per-slot collision dispatch trace. |
| Final portable cohort replay | The four formal-source cohorts had been produced at different development stages. | A single content-addressed 114-file candidate replayed Controlled-60, External-40, External-25, and Rare-40. | 165/165 final verdicts were reproduced; 163/165 match binary oracle labels. The two differences are the retained External-25 UNK outcomes and remain blocked. |
| Historical OpenArm collision evidence | C04 and C05 exceeded the 25 ms post-run host-receive coverage limit, and the complete historical per-slot verdict-to-release-to-send join was not retained. | Frozen C/H/R programs, trajectories, and contracts were revalidated offline with the final build; supplementary collision confirmations were not substituted for primary slots. | All five collision controller tasks completed and all five sampled-state checks passed. Between-sample evidence remains 3 PASS and 2 coverage UNKNOWN, with gaps of 41.55 ms and 61.32 ms. Campaign-level P044 provenance is supported and no retained non-VWB send was found; direct per-slot tokenized enforcement remains unclaimed. |

The correction ledger is evidence about provenance and scope. It must not be
used to relabel a historical trial, replace an unfavorable slot, or imply
population-level reliability.
